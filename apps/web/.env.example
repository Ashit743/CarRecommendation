# Base URL for API calls. If served on the same domain, keep it as '/api'.
# If you are hosting the API on a separate domain, set it to the full URL (e.g., https://api.yourdomain.com/api)
VITE_API_URL=/api
